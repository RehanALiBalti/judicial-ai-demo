# Copy to /opt/jams/.env and adjust values
# systemd service loads this via EnvironmentFile=

HOST=127.0.0.1
PORT=8000

# Comma-separated origins (only needed if frontend is on another domain)
CORS_ORIGINS=https://your-domain.com,http://your-domain.com

OLLAMA_URL=http://127.0.0.1:11434/api/generate
OLLAMA_MODEL=qwen2.5:1.5b

EMBEDDING_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2

# Cache dirs for www-data (Hugging Face / torch — do NOT use /var/www/.cache)
HOME=/opt/jams/.home
JAMS_CACHE_DIR=/opt/jams/.cache
XDG_CACHE_HOME=/opt/jams/.cache
HF_HOME=/opt/jams/.cache/huggingface
TRANSFORMERS_CACHE=/opt/jams/.cache/huggingface
TORCH_HOME=/opt/jams/.cache/torch
SENTENCE_TRANSFORMERS_HOME=/opt/jams/.cache/sentence_transformers

# Optional: Hugging Face token for faster embedding model download
# HF_TOKEN=
