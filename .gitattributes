* text=auto eol=lf
*.sh text eol=lf

# Indexed AI store (large — use Git LFS when pushing after --index-only)
data/jams_store.json filter=lfs diff=lfs merge=lfs -text
data/chroma/** filter=lfs diff=lfs merge=lfs -text

# LHC/FCCP PDFs + manifests: regular git (~2 GB repo, no LFS quota needed)
